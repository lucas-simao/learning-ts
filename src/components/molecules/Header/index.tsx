import React, { VFC } from 'react'
import './index.scss'

const Header: VFC = () => {
  return (
    <header>
      <section>
        <h1>React with Typescript</h1>
      </section>
    </header>
  )
};

export default Header;